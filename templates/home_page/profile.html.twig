{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h3>Update Account</h3>
        <a href="{{path('home_page')}}"><button type="button" class="btn btn-outline-primary">Back to index</button></a>
    </div>
<div class="container">
{{ form_start(form) }}
{{ form_row(form.username) }}
{{ form_end(form) }}
</div>
<br>
{% endblock %}
